import React from 'react';
import Styles from './Spinner.module.css'; // Ensure this CSS file is created and imported

const Spinner = () => {
  return <div className={Styles.spinner}></div>;
};

export default Spinner;
